{
  "name": "simple-event-tracer",
  "version": "1.0.0",
  "description": "A helper package, creates structured event logs so the logs can be combined to a trace later",
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "json-stringify-safe": "^5.0.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.2.2",
    "nyc": "^14.1.1",
    "rewire": "^5.0.0"
  },
  "scripts": {
    "test": "mocha test/",
    "coverage": "nyc --reporter=html mocha test/"
  },
  "keywords": [
    "tracing",
    "logging"
  ],
  "author": "masteris@gmail.com",
  "license": "MIT",
  "engines": {
    "node": ">=12"
  }
}
